using UnityEngine;

public class Script : MonoBehaviour
{
  
    float velocityPaddelRed = 2.5f;
    float velocityBall = 3.5f;
    int point = 0;
    string swapball;
    bool ballInMiddle = true;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        float velocityPaddelBlue = 2.5f;
        int ballQuantity = 1;
        string paddelRed;
        bool paddelBlueInUp = false;
        bool paddelRedInDown = true;
        bool masterKey = true;
        bool heroIsLevel5 = false;
        bool passLevel;

     
        

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
